{
  "name": "extensions-manager",
  "version": "0.0.1",
  "description": "A chrome extension for extensionsManager.",
  "scripts": {
    "clean": "rimraf build/",
    "build:static": "xcopy /E /I public build\\",
    "dev": "npm run clean && npm run build:static && set NODE_ENV=development && node build.js",
    "build": "npm run clean && npm run build:static && set NODE_ENV=production && node build.js",
    "prepack": "npm run build",
    "pack": "npm run build && 7z a build/build.zip ./build/*"
  },
  "keywords": [
    "chrome-extension"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ZhangYiBo1998/extensionsManager-chrome-extension.git"
  },
  "author": "zhangyibo",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/ZhangYiBo1998/extensionsManager-chrome-extension/issues"
  },
  "homepage": "https://github.com/ZhangYiBo1998/extensionsManager-chrome-extension",
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.42",
    "@types/react": "^18.3.29",
    "@types/react-dom": "^18.3.7",
    "esbuild": "^0.28.0",
    "rimraf": "^6.1.3",
    "sass": "^1.100.0",
    "typescript": "^6.0.3"
  }
}
